---

---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Word Battle - Multiplayer Game</title>
    <link href="/src/styles/styles.css"rel="stylesheet" type="text/css">
	<script type="module" src="/src/client/main" defer></script>
</head>
<body>
    <div class="container">
        <h1>Word Battle</h1>
        
        <!-- Home Screen -->
        <div id="home-screen" class="screen active">
            <div class="input-group">
                <label for="player-name">Your Name</label>
                <input type="text" id="player-name" placeholder="Enter your name" maxlength="20">
            </div>
            <button onclick="showCreateRoom()">Create Room</button>
            <button onclick="showJoinRoom()">Join Room</button>
        </div>

        <div id="create-room-screen" class="screen">
            <h2>Create a Room</h2>
            <div class="input-group">
                <label for="room-name">Room Name (Optional)</label>
                <input type="text" id="room-name" placeholder="Enter room name" maxlength="30">
            </div>
            <button onclick="createRoom()">Create Room</button>
            <button onclick="showHome()">Back</button>
        </div>


		<div id="join-room-screen" class="screen">
            <h2>Join a Room</h2>
            <div class="input-group">
                <label for="join-code">Room Code</label>
                <input type="text" id="join-code" placeholder="Enter 6-digit code" maxlength="6">
            </div>
            <button onclick="joinRoom()">Join Room</button>
            <button onclick="showHome()">Back</button>
        </div>
		

        <div id="message-area"></div>
    </div>

</body>
</html>